# 黑神话悟空 - 系统架构设计文档

> 版本: 1.0  
> 最后更新: 2024年12月5日

---

## 📋 目录

1. [项目概述](#项目概述)
2. [架构总览](#架构总览)
3. [核心组件设计](#核心组件设计)
4. [接口设计](#接口设计)
5. [角色类层次结构](#角色类层次结构)
6. [系统交互流程](#系统交互流程)
7. [实现状态与路线图](#实现状态与路线图)

---

## 项目概述

**游戏类型**: Soul-like / 黑神话 风格动作 RPG

**核心特性**:
- 硬核战斗系统（攻击、闪避、格挡、弹反）
- 锁定目标机制
- 体力管理系统
- Boss 战斗
- 状态效果（Buff/Debuff）

**设计原则**:
- **组件化**: 功能通过可复用组件实现
- **接口驱动**: 通过接口解耦，而非具体类依赖
- **数据驱动**: 数值配置与逻辑分离

---

## 架构总览

### 高层架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            游戏框架层                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  GameMode   │  │  GameState  │  │PlayerController│ │  HUD       │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            接口层 (Interfaces)                           │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐            │
│  │ ICombat   │  │ITargetable│  │ IDamageable│  │IStateControl│           │
│  │ 战斗行为   │  │ 可被锁定  │  │  可受伤害  │  │  状态控制   │           │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            组件层 (Components)                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        核心战斗组件                               │   │
│  │  HealthComponent │ StaminaComponent │ CombatComponent            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        交互组件                                   │   │
│  │  HitboxComponent │ TargetingComponent │ TeamComponent            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        高级系统组件                               │   │
│  │  StatusEffectComponent │ EquipmentComponent │ InventoryComponent │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            角色层 (Characters)                           │
│                                                                         │
│                        ┌─────────────────┐                              │
│                        │ BlackMythCharacter│                             │
│                        │   (抽象基类)      │                              │
│                        └────────┬────────┘                              │
│                                 │                                       │
│          ┌──────────────────────┼──────────────────────┐                │
│          ▼                      ▼                      ▼                │
│  ┌───────────────┐    ┌───────────────┐    ┌───────────────┐           │
│  │ WukongCharacter│    │   EnemyBase   │    │    NPC Base   │           │
│  │   (玩家角色)   │    │  (敌人基类)   │    │   (NPC基类)   │           │
│  └───────────────┘    └───────┬───────┘    └───────────────┘           │
│                               │                                         │
│                    ┌──────────┼──────────┐                              │
│                    ▼          ▼          ▼                              │
│              ┌─────────┐ ┌─────────┐ ┌─────────┐                        │
│              │ 小兵敌人 │ │ 精英敌人 │ │  Boss   │                        │
│              └─────────┘ └─────────┘ └─────────┘                        │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            场景/系统层                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │TriggerZone  │  │CombatArena  │  │ Checkpoint  │  │ Spawner     │    │
│  │ 触发区域    │  │ 战斗区域管理 │  │  检查点     │  │  刷怪点     │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 核心组件设计

### 🔴 第一优先级 - 战斗核心

#### 1. HealthComponent (已完成 ✅)
**职责**: 管理生命值、受伤、死亡

```cpp
// 核心接口
void TakeDamage(float Damage, AActor* Instigator);
void Heal(float Amount);
bool IsAlive() const;
float GetHealthPercent() const;

// 委托
FOnHealthChanged OnHealthChanged;
FOnDeath OnDeath;
FOnDamageTaken OnDamageTaken;
```

#### 2. StaminaComponent (已完成 ✅)
**职责**: 管理体力值、消耗、恢复

```cpp
// 核心接口
bool ConsumeStamina(float Amount);
bool HasEnoughStamina(float Amount) const;
void SetContinuousConsumption(bool bEnable, float Rate);

// 委托
FOnStaminaChanged OnStaminaChanged;
FOnStaminaDepleted OnStaminaDepleted;
```

#### 3. CombatComponent (已完成 ✅)
**职责**: 管理攻击力、连击、伤害计算

```cpp
// 核心接口
float CalculateDamage(bool bHeavy, bool bAir, int ComboIndex) const;
void AdvanceCombo();
void ResetCombo();

// 委托
FOnDamageDealt OnDamageDealt;
```

#### 4. TeamComponent (待实现 📝)
**职责**: 阵营管理、敌我判定

```cpp
// 核心接口
UENUM(BlueprintType)
enum class ETeam : uint8
{
    Player,      // 玩家阵营
    Enemy,       // 敌人阵营
    Neutral,     // 中立
    Environment  // 环境（陷阱等）
};

ETeam GetTeam() const;
void SetTeam(ETeam NewTeam);
bool IsHostileTo(ETeam OtherTeam) const;
bool IsHostileToActor(AActor* OtherActor) const;
```

#### 5. HitboxComponent → TraceHitboxComponent (需重构 🔄)
**职责**: 攻击碰撞检测

**当前问题**: 使用 Box 碰撞，无法精确跟踪武器

**建议方案**: 改用 Trace 射线检测

```cpp
// 新设计
UPROPERTY()
FName StartSocketName = "WeaponStart";  // 武器起点 Socket
FName EndSocketName = "WeaponEnd";      // 武器终点 Socket
float TraceRadius = 20.0f;              // 扫描半径

// 核心接口
void ActivateTrace();
void DeactivateTrace();
void PerformTrace();  // 每帧调用，从 Start 到 End 做球形扫描

// 委托
FOnHitDetected OnHitDetected;
```

---

### 🟡 第二优先级 - 战斗增强

#### 6. TargetingComponent (待实现 📝)
**职责**: 锁定目标系统

```cpp
// 核心接口
void ToggleLockOn();
void SwitchTarget(bool bRight);
AActor* GetLockedTarget() const;
bool IsTargeting() const;

// 配置
float TargetingDistance = 1500.0f;
float TargetingRadius = 500.0f;

// 委托
FOnTargetChanged OnTargetChanged;
FOnTargetLost OnTargetLost;
```

#### 7. HitReactionComponent (待实现 📝)
**职责**: 受击反馈、硬直、击退

```cpp
// 核心接口
void ApplyHitReaction(EHitReactionType Type, FVector HitDirection);
void ApplyKnockback(FVector Direction, float Force);
bool CanReceiveHitReaction() const;

// 配置
float PoiseValue = 100.0f;           // 霸体值
float PoiseRegenRate = 20.0f;        // 霸体恢复速度
float HitStunDuration = 0.3f;        // 硬直时间

// 委托
FOnHitReaction OnHitReaction;
FOnPoiseBreak OnPoiseBreak;
```

#### 8. BlockComponent (待实现 📝)
**职责**: 格挡、弹反

```cpp
// 核心接口
void StartBlock();
void EndBlock();
bool IsBlocking() const;
bool TryParry();  // 尝试弹反

// 配置
float BlockDamageReduction = 0.7f;   // 格挡减伤 70%
float ParryWindow = 0.2f;            // 弹反窗口（秒）
float StaminaCostOnBlock = 20.0f;    // 格挡消耗体力

// 委托
FOnBlockSuccess OnBlockSuccess;
FOnParrySuccess OnParrySuccess;
FOnGuardBreak OnGuardBreak;
```

---

### 🟢 第三优先级 - 系统扩展

#### 9. StatusEffectComponent (待实现 📝)
**职责**: Buff/Debuff 管理

```cpp
// 状态效果类型
UENUM(BlueprintType)
enum class EStatusEffect : uint8
{
    None,
    Burning,     // 燃烧
    Frozen,      // 冰冻
    Poisoned,    // 中毒
    Bleeding,    // 流血
    Stunned,     // 眩晕
    Buffed       // 增益
};

// 核心接口
void ApplyEffect(EStatusEffect Effect, float Duration, float Intensity);
void RemoveEffect(EStatusEffect Effect);
bool HasEffect(EStatusEffect Effect) const;
TArray<FActiveStatusEffect> GetActiveEffects() const;
```

#### 10. EquipmentComponent (待实现 📝)
**职责**: 装备管理

```cpp
// 核心接口
void EquipWeapon(ABaseWeapon* Weapon);
void UnequipWeapon();
ABaseWeapon* GetCurrentWeapon() const;
void SwitchWeapon(int SlotIndex);
```

#### 11. ThreatComponent (待实现 📝)
**职责**: AI 仇恨管理

```cpp
// 核心接口
void AddThreat(AActor* Source, float Amount);
void RemoveThreat(AActor* Source);
AActor* GetHighestThreatTarget() const;
void ClearAllThreats();

// 委托
FOnThreatAdded OnThreatAdded;
FOnThreatCleared OnThreatCleared;
```

---

## 接口设计

### ICombatant - 战斗者接口
```cpp
UINTERFACE(MinimalAPI)
class UCombatant : public UInterface
{
    GENERATED_BODY()
};

class ICombatant
{
    GENERATED_BODY()

public:
    // 能否受到伤害
    virtual bool CanReceiveDamage() const = 0;
    
    // 设置无敌帧
    virtual void SetInvincible(bool bInvincible) = 0;
    
    // 获取阵营
    virtual ETeam GetTeam() const = 0;
};
```

### ITargetable - 可锁定目标接口
```cpp
UINTERFACE(MinimalAPI)
class UTargetable : public UInterface
{
    GENERATED_BODY()
};

class ITargetable
{
    GENERATED_BODY()

public:
    // 能否被锁定
    virtual bool CanBeTargeted() const = 0;
    
    // 被锁定时的回调
    virtual void OnTargeted(bool bIsTargeted) = 0;
    
    // 获取锁定点位置
    virtual FVector GetTargetLocation() const = 0;
};
```

### IDamageable - 可受伤接口
```cpp
UINTERFACE(MinimalAPI)
class UDamageable : public UInterface
{
    GENERATED_BODY()
};

class IDamageable
{
    GENERATED_BODY()

public:
    // 接收伤害
    virtual void ReceiveDamage(const FDamageInfo& DamageInfo) = 0;
    
    // 是否存活
    virtual bool IsAlive() const = 0;
};
```

---

## 角色类层次结构

```
ACharacter (UE 基类)
    │
    └── ABlackMythCharacter (项目基类)
            │
            │   // 通用组件
            │   ├── HealthComponent
            │   ├── TeamComponent
            │   └── HitReactionComponent
            │
            ├── AWukongCharacter (玩家)
            │       │
            │       │   // 玩家专用组件
            │       ├── StaminaComponent
            │       ├── CombatComponent
            │       ├── TargetingComponent
            │       ├── BlockComponent
            │       └── HitboxComponent
            │
            └── AEnemyBase (敌人基类)
                    │
                    │   // 敌人通用组件
                    ├── ThreatComponent
                    ├── CombatComponent
                    └── HitboxComponent
                    │
                    ├── ARegularEnemy (普通敌人)
                    │
                    ├── AEliteEnemy (精英敌人)
                    │       └── StatusEffectComponent
                    │
                    └── ABossEnemy (Boss)
                            ├── 多阶段战斗逻辑
                            └── 专属 UI (Boss 血条)
```

---

## 系统交互流程

### 攻击流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        玩家按下攻击键                             │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ StaminaComponent.HasEnoughStamina(AttackCost) ?                   │
│     ├── No  → 取消攻击，播放疲惫音效                               │
│     └── Yes → 继续                                                │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ 播放攻击蒙太奇 → AnimNotify: ActivateHitbox                       │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ HitboxComponent.PerformTrace() 检测命中                           │
│     ├── 命中目标 → 检查 TeamComponent.IsHostileTo()               │
│     │     ├── 友方 → 忽略                                         │
│     │     └── 敌方 → 继续                                         │
│     └── 未命中 → 等待下一帧                                        │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ CombatComponent.CalculateDamage() 计算伤害                        │
│     └── 构建 FDamageInfo 结构                                     │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ 目标.IDamageable.ReceiveDamage(DamageInfo)                        │
│     ├── 检查格挡 → BlockComponent.IsBlocking()                    │
│     │     ├── 格挡成功 → 减伤，消耗体力                            │
│     │     └── 弹反成功 → 施加硬直给攻击者                          │
│     ├── HealthComponent.TakeDamage()                              │
│     └── HitReactionComponent.ApplyHitReaction()                   │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ HealthComponent.CurrentHealth <= 0 ?                              │
│     ├── No  → 播放受击动画                                        │
│     └── Yes → 触发 OnDeath，播放死亡动画                           │
└───────────────────────────────────────────────────────────────────┘
```

### Boss 战触发流程

```
┌─────────────────────────────────────────────────────────────────┐
│ 玩家进入 Boss 房间 (TriggerZone)                                 │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ TriggerZone.OnActorEntered                                        │
│     └── 检查 TeamComponent.GetTeam() == Player                    │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ CombatArenaManager.StartBossFight()                               │
│     ├── 封锁出口（关门）                                           │
│     ├── 播放 Boss 登场动画                                         │
│     ├── 显示 Boss 血条 UI                                          │
│     └── Boss.ThreatComponent.AddThreat(Player, 100)               │
└───────────────────────────────┬───────────────────────────────────┘
                                ▼
┌───────────────────────────────────────────────────────────────────┐
│ Boss AI 开始行动                                                   │
│     └── 追踪 ThreatComponent.GetHighestThreatTarget()             │
└───────────────────────────────────────────────────────────────────┘
```

---

## 实现状态与路线图

### 当前状态

| 组件/系统 | 状态 | 负责人 | 备注 |
|-----------|------|--------|------|
| HealthComponent | ✅ 完成 | - | 功能完整 |
| StaminaComponent | ✅ 完成 | - | 功能完整 |
| CombatComponent | ✅ 完成 | - | 功能完整 |
| HitboxComponent | ⚠️ 基础完成 | - | 需改为 Trace 方式 |
| TeamComponent | 📝 待实现 | - | 高优先级 |
| TargetingComponent | 📝 待实现 | - | 高优先级 |
| TriggerZoneComponent | 📝 待实现 | - | Boss 战需要 |
| EnemyBase | 🔨 开发中 | 队友 | 另一分支 |
| BossEnemy | 📝 待实现 | - | 依赖 EnemyBase |

### 开发路线图

```
Phase 1: 核心战斗 (当前)
├── [x] HealthComponent
├── [x] StaminaComponent  
├── [x] CombatComponent
├── [ ] TeamComponent
├── [ ] TraceHitboxComponent (重构)
└── [ ] 基础敌我交互

Phase 2: 战斗增强
├── [ ] TargetingComponent (锁定系统)
├── [ ] HitReactionComponent (受击反馈)
├── [ ] BlockComponent (格挡/弹反)
└── [ ] 连招系统完善

Phase 3: 敌人系统
├── [ ] EnemyBase 整合
├── [ ] ThreatComponent (仇恨)
├── [ ] AI 行为树
└── [ ] Boss 战框架

Phase 4: 场景系统
├── [ ] TriggerZoneComponent
├── [ ] CombatArenaManager
├── [ ] CheckpointSystem
└── [ ] 过场动画触发

Phase 5: 扩展系统
├── [ ] StatusEffectComponent
├── [ ] EquipmentComponent
├── [ ] InventoryComponent
└── [ ] 存档系统
```

---

## 附录

### A. 命名规范

| 类型 | 前缀 | 示例 |
|------|------|------|
| Actor | A | ABlackMythCharacter |
| Component | U | UHealthComponent |
| Interface | I | ITargetable |
| Enum | E | ETeam |
| Struct | F | FDamageInfo |
| Delegate | F...Signature | FOnHealthChangedSignature |

### B. 文件结构

```
Source/BlackMyth/
├── BlackMyth.Build.cs
├── BlackMyth.h/cpp
├── Components/
│   ├── HealthComponent.h/cpp
│   ├── StaminaComponent.h/cpp
│   ├── CombatComponent.h/cpp
│   ├── TeamComponent.h/cpp         (待创建)
│   └── TargetingComponent.h/cpp    (待创建)
├── Combat/
│   ├── DamageTypes.h
│   ├── HitboxComponent.h/cpp
│   ├── TraceHitboxComponent.h/cpp  (待创建)
│   └── AnimNotify_*.h/cpp
├── Characters/
│   ├── BlackMythCharacter.h/cpp
│   ├── WukongCharacter.h/cpp
│   └── EnemyBase.h/cpp             (队友分支)
├── Interfaces/
│   ├── Combatant.h
│   ├── Targetable.h
│   └── Damageable.h
├── AI/
│   └── (AI 控制器、行为树任务)
├── UI/
│   ├── PlayerHUDWidget.h/cpp
│   └── BlackMythHUD.h/cpp
└── Systems/
    ├── TriggerZone.h/cpp           (待创建)
    └── CombatArenaManager.h/cpp    (待创建)
```

### C. 参考资料

- [MeleeCombatSystem 项目分析](#) - 同类 Soul-like 项目参考
- [UE5 Gameplay Ability System](https://docs.unrealengine.com/5.0/en-US/gameplay-ability-system-for-unreal-engine/) - 官方 GAS 文档
- [UE5 AI 文档](https://docs.unrealengine.com/5.0/en-US/artificial-intelligence-in-unreal-engine/) - 行为树参考

---

> 📝 **注**: 此文档会随项目进展持续更新。